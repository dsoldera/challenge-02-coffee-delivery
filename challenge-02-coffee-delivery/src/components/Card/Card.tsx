import { CardProps } from '../../types/Card';
import { Container } from './styles';

export const Card = ({ coffee }: CardProps) => {
  return (
    <Container>
      <h2>Card</h2>
    </Container>
  )
}